# Media Evidence

This directory is reserved for curated public-facing screenshots and short demo
captures used by `README.md` and portfolio links.

Current curated files:

- `demo-full.mp4`: full capture linked from README. Keep it reasonably small or
  move it to a GitHub Release if the repo becomes too heavy.
- `demo-main.gif`: short loop showing the 10,000-cube scene, HUD, draw-mode
  toggle, and character runtime.
- `demo-main.png`: still fallback for GitHub preview or PDF links.

Optional next capture:

- `hud-uploadarena.png`: HUD screenshot with UploadArena metrics visible.

Do not store raw KCC traces, private debugging dumps, personal portfolio drafts,
or reference-engine material here.
